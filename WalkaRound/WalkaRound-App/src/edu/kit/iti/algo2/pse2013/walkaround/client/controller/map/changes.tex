Changelog mapController

public static MapController initialize(MapView) hinzugefügt
der Map Controller braucht eine Referenz auf den MapView,
der aus Android spezifischen Gründen zuerst gestartet werden muss
und nicht als Singelton initialisiert werden kann.

public PullUpView getPullUpView() hinzugefügt.
Der headUpController benötigt eine Referenz auf die PulUpView

public float getCurrentLevelOfDetail() 
Der headUpController benötigt eine Möglichkeit auf den aktuellen Level of Detail zuzugreifen.

public List<DisplayWaypoint> getCurrentRoute()
das MapModel braucht Zugriff auf die derzeit in Display Coordinates umgewandelten Coordinaten


public void onMapOverlayImageChange(Bitmap b)
public void onRouteOverlayImageChange(Bitmap b) 

Android kennt BufferedImage nicht. Daher verwenden wir das Android Prinzip Bitmap.

public void onShift(float distanceX, float distanceY)
lässt sich einfacher übergeben